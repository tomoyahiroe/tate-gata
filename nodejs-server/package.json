{
  "name": "nodejs-server",
  "version": "1.0.0",
  "main": "dist/index.js",
  "author": "tomoyahiroe",
  "license": "MIT",
  "scripts": {
    "start": "nodemon dist/index.js",
    "build": "npx tsc --outDir dist/ -p ./tsconfig.json",
    "build:watch": "npx tsc -w --outDir dist/ -p ./tsconfig.json",
    "dev": "npm run build:watch & npm run start"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.9.0",
    "nodemon": "^3.0.1",
    "prettier": "^3.0.3",
    "ts-node": "^10.9.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.2.2"
  }
}
